# TODO
- Add a test that compares Butterworth filter output against a Python SciPy `butter` + `filtfilt` reference and asserts the difference is within tolerance.
- Add a test that compares C++ mel-spectrogram outputs (log/PCEN) against Python (librosa) reference results within tolerance.
